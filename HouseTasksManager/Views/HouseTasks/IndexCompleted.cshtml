@model IEnumerable<HouseTasksManager.Models.HouseTask>

<h4 class="text-center">Completed Tasks</h4>

<div class="d-flex justify-content-between mt-3">
    <span>
        <a class="btn btn-secondary" asp-action="Index">Unassigned Tasks</a>
    </span>
    <span>
        <a class="btn btn-secondary" asp-action="IndexAssigned">Assigned Tasks</a>
    </span>
    <span>
        <a class="btn btn-primary" asp-action="IndexCompleted">Completed Tasks</a>
    </span>
</div>

<table class="table table-bordered table-hover mt-3">
    <thead>
        <tr class="text-center">
            <th>
                @Html.DisplayNameFor(model => model.Description)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Owner)
            </th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                @if (item.Finished)
                {
                    <td>
                        @Html.DisplayFor(modelItem => item.Description)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.Owner)
                    </td>
                }
            </tr>
        }
    </tbody>
</table>